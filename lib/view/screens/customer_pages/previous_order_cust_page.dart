import 'package:flutter/material.dart';

class PreviousOrderCustomer extends StatelessWidget {
  const PreviousOrderCustomer({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Previous Orders')),
    );
  }
}
